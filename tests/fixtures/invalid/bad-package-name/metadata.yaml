# Bad app_id - contains uppercase letters which are not allowed
name: Bad App ID App
app_id: Bad-App-Id  # Uppercase not allowed in app_id
version: 1.0.0
description: Test app with invalid app_id
homepage: https://example.com
maintainer: Test Developer <test@example.com>
license: MIT
tags:
  - role::container-app
debian_section: net
architecture: all

web_ui:
  enabled: true
  path: /
  port: 8080
  protocol: http

default_config:
  PORT: "8080"
